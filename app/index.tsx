import { Button, Text, View } from "react-native";

export default function Index() {
  return (
    <View className="flex-1 justify-center items-center">
      <Text>Logo</Text>
      <Button title="Continue With TikTok" onPress={() => {}} />
    </View>
  );
}
